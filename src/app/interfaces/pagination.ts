export interface Pagination {
    recordsPerPageOptions: any;
    recordsPerPage: number;
    totalRecords: number;
    totalPages: number;
    currentPage: number;
    firstPage: number;
    prevPage: number;
    nextPage: number;
    lastPage: number;
}
